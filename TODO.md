# TODO

- [ ] Add example `config.example.disp` to repository
- [ ] Add entrypoint to build an application binary
- [ ] Add `Makefile` to build the application binary
- [ ] Add user `XDG_USER_CONFIG` directory as a default configuration path
- [ ] Implement ordering capabilities on rules, or at least make it so the last define rule is the default one
- [ ] Make an interactive layout selector based on configured hashes
